{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editer un compte</h1>
    {{ form(edit_form) }}
    {{ form(delete_form) }}
    <a class="btn btn-default btn-sm" href="{{ path('accounting_account_homepage') }}">Retour</a>
{% endblock %}
